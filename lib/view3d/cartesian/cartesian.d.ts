export declare class Cartesian {
    private static _offsetLongitude;
    private static _offsetLatitude;
    static offset2(longitude: number, latitude: number): {
        longitude: number;
        latitude: number;
    };
}
